import mini/pexp/parse;
import fs/filesystem;
//import mini/pexp/interpret;
//import mini/pexp/compile;



main() {
	if (false) {
		file0 = "mini/tests/positions.flow";
		file = "c:/flow9/lib/formats/unicode/unidecode.flow";
		code = getFileContent(file);
		p = parsePExp(code, println);
	} else {
		files = filter(readDirectoryRecursively("c:\\flow9\\tools"), \f -> endsWith(f, ".flow"));
		t = timestamp();

		errors = ref 0;
		iter(files, \file -> {
			if (^errors < 10) {
				code = getFileContent(file);
				p = parsePExp(code, \error -> {
					errors := ^errors + 1;
					println(file + ":" + error)
				});
			}
		});	

		println(timestamp() - t);
	}

	// println(p);
//	va = interpretPExp(makeTree(), p);
//	println(va);
//	cpp = pexp2cpp(p, true);
//	println(cpp);
	quit(0);
}
