import mini/types/type2;

export {
	// This is the typed AST we send to the backends when they need to compile
	BProgram(declarations : [BDeclaration]);

	BDeclaration ::= BFunction, BGlobalVar, BNative, BComment;

	BFunction(name : string, args : [BFunArg], body : BStatement, type : MiniType2);
		BFunArg(name : string);

	BGlobalVar(name : string, body : BExp, type : MiniType2);

	BNative(name : string, isio : bool, type : MiniType2, nativeName : string);

	BComment(comment : string);

	// Statement-based languages can get this format:
	BStatement ::= BIfStatement, BLetStatement, BReturnVoid, BReturn, BSequenceStatement, BComment;
		BIfStatement(cond : BExp, then : BStatement, else_ : BStatement);
		BLetStatement(name : string, value : BExp, body : BStatement);
		BSequenceStatement(statements : [BStatement]);

		// The empty return. We could maybe have BVoid and BVoidType instead?
		BReturnVoid();
		// Return a value
		BReturn(val : BExp);
		// TODO: We need a construct for expressions that are void at the statement level

	BExp ::= BValue, BVar, BLet, BLambda, BCall, BIf, BSequence, BComment;
		BVar(name : string, type : MiniType2);
		// The type is the type of the body
		BLet(name : string, value : BExp, body : BExp, type : MiniType2);

		BLambda(args : [BFunArg], body : BExp, type : MiniType2);

		BCall(fn : BExp, args : [BExp], type : MiniType2);
		BIf(cond : BExp, then : BExp, else_ : BExp, type : MiniType2);
		BSequence(exps : [BExp], type : MiniType2);

	// The backends get this language
	BValue ::= BInt, BDouble, BString;
		BInt(value : int, type : MiniType2);
		BDouble(value : double);
		BString(value : string);

}
