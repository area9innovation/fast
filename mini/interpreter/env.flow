import mini/exp/ast;

export {
	MiniInterpreter(
		env : MiniAst,
		nextRefId : ref int,
		refs : ref Tree<int, MiniExp>,
		// What number should the next dynamic variable have?
		nextDynId : ref int,
		// Lookup from logical name to generated name
		dynamicVars : ref Tree<string, string>,
	);
}
