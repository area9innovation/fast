import mini/mini_compiler;

main() {
	db = MiniDb(
		["."], 
		[MiniFilename("mini/tests/test.mini")], 
		MiniAst(makeTree(), makeTree()),
		makeList(), 
		println
	);

	fdb = miniCompile(db);
	iterList(fdb.stack, println);
	quit(0);
}
