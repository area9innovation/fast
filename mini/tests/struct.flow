native println : io (flow) -> void = Native.println;

None();
Some(v : int);
Maybe ::= None, Some;

main() {
	None();
	s = Some(1);
	prinln(s.v);
	println(switch (s) {
		None(): 0;
		Some(v): v;
	});
	println(s ?? {
		1;
	} : 0);

	Some(s with value = 2);
}
