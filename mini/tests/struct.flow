native println : io (flow) -> void = Native.println;

Maybe ::= None, Some;
	None();
	Some(value : int);

main() {
	None();
	s = Some(1);
	println(s.value);
	m : Maybe = s;
	println(switch (m) {
		None(): 0;
		Some(v): v;
	});
//	println(s ?? { 1; } : 0);

	e = Some(s with value = 2);
	e;
}
