import string;
main() {
	1;
	{};
	3;
	isPalindromic("foo");
	if (false) {
		0;
	} else {
		1;
	}
}

isPalindromic(s : string) -> bool {
	l = strlen(s);
    l <= 1 || {
		firste = strLeft(s, 1);
		last = strRight(s, l - 1);
        firste == last && isPalindromic(strsubsmart(s, 1, -1));
    }
}
