import mini/types/type;
import ds/set;

export {
	MTyvarRequirements(
		// What are the transitive lower bounds?
		lower : Set<MiniType>,
		// What are the transitive upper bounds?
		upper : Set<MiniType>,
	);
}
