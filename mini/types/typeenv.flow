import mini/mini_db;
import mini/types/mini_type2;

export {
	MiniTypeEnv(
		ast : MiniAst,
		types : Tree<string, MiniType2>,
		// What are the tyvars bound to?
		mutable tyvars : Tree<int, MiniType2>,

		// For the MLSub algorithm add these:
		tyvarLowerBounds : Tree<int, [MiniType2]>,
		tyvarUpperBounds : Tree<int, [MiniType2]>,

		// What is the next tyvar id?
		mutable tyvarId : int,
	);
}
