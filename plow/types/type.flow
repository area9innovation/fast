export {
	TType ::= TTypeName, TTypeVar, TTypeFunction, TTypeOverload;
		TTypeVar(id : int);
		TTypeName(id : string, typars : [TType]);
		TTypeFunction(args : [TType], returnType : TType);
		TTypeOverload(overloads : [TType]);
}
